<% content_for :content_header  do %>
    <section class="content-header" >
      <h1>Marca</h1>

      <ol class="breadcrumb">
        <li><a href="/"><i class="fa fa-dashboard"></i> Marca</a></li>
        <li><%= link_to "Marca", brands_path %></li>
        <li class="active">Mostrar</li>
      </ol>
    </section>
<% end %>

<section class="content invoice botones">

 <p>
    <strong>Nombre:</strong>
    <%= @brand.name %>
  </p>
  
          <%if (current_user.admin) %>

  <%= link_to 'Editar', edit_brand_url,:class =>"btn btn-warning" %>
           <%end%>

  <%= link_to 'Atras', brands_url,:class =>"btn btn-info" %>
</section>