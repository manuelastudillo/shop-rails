<div class="panel panel-default">
  <div class="panel-heading">Ventas</div>
  <div class="panel-body">
    <%= render partial: 'layouts/search_form', locals: { search_entity: 'sales', word: "Venta" } %>
    <br>
    <div class="col-md-5 col-md-offset-5 col-sm-5 col-sm-offset-5">
      <p>
        <%= button_to new_sale_path, {method: :get, class: "btn btn-success"} do %>
          <span class="glyphicon glyphicon-plus" aria-hidden="true"> </span> Nueva Venta
        <% end %>
      </p>
                                          <div style="margin-top: 40px;">
       <%= link_to " Imprimir Productos", sales_path(@sale, format: "pdf") %>
              </div>
    </div>
  </div>
  <%= render partial: 'results_search' %>
</div>